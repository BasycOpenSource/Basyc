# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [BasycPullRequestPipeline (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_BasycPullRequestPipeline --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: pullRequest

on:
  pull_request:
    branches:
      - develop
      - main

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v3
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd StaticCodeAnalysisAll UnitTestAll'
        run: ./build.cmd StaticCodeAnalysisAll UnitTestAll
        env:
          nugetApiKey: ${{ secrets.NUGET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          nugetSource: ${{ vars.NUGET_SOURCE }}
