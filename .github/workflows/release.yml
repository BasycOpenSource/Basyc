# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: release

on:
  pull_request:
    branches:
      - main

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd StaticCodeAnalysisAll UnitTestAll NugetPushAll'
        run: ./build.cmd StaticCodeAnalysisAll UnitTestAll NugetPushAll
        env:
          NuGetApiKey: ${{ secrets.NUGET_API_KEY }}
          NuGetApiPrivateKeyPfxBase64: ${{ secrets.NUGET_API_PRIVATE_KEY_PFX_BASE64 }}
          NuGetApiCertPassword: ${{ secrets.NUGET_API_CERT_PASSWORD }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
