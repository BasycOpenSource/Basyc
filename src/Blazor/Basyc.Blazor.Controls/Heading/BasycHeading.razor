@namespace Basyc.Blazor.Controls

<basycLabelView>
    @GetLabelTags2(Size, ChildContent)
</basycLabelView>

@code {
    [Parameter] public HeadingSize Size { get; set; }


    public static string GetHTagNumber(HeadingSize labelSize)
    {
        var number =  labelSize switch
        {
            HeadingSize.H1 => 1,
            HeadingSize.H2 => 2,
            HeadingSize.H3 => 3,
            HeadingSize.H4 => 4,
            HeadingSize.H5 => 5,
            HeadingSize.H6 => 6,
            _ => throw new ArgumentException(nameof(labelSize))
        };
        return number.ToString();
    }

    public RenderFragment GetLabelTags2(HeadingSize labelSize, RenderFragment? content)
    {
        RenderFragment finalContent = builder =>
        {
            builder.OpenElement(1, $"h{GetHTagNumber(Size)}");
            builder.AddContent(2, content);
            builder.CloseElement();
        };

        return finalContent;
    }


    [Parameter]
    public RenderFragment? ChildContent { get; set; }


}
